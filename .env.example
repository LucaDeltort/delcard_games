# URL of the Cloudflare Worker that generates TURN credentials.
# Without this, PeerJS uses STUN only — connections may fail on iOS with Private Relay.
PUBLIC_TURN_CREDS_URL=https://delcard-turn-creds.<your-subdomain>.workers.dev
